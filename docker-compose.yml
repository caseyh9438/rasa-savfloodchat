version: '3.0'
services:
  rasa:
    image: rasa/rasa:latest-full
    ports:
      - 5005:5005
    volumes:
      - ./:/app
    command: run --enable-api --credentials credentials.yml

  action_server:
    build: .
    volumes:
      - ./actions:/app/actions